$ composer create-project laravel/laravel testetecnico
$ git init
$ git add .
$ git commit -m 'Initial Commit'
$ composer install
$ composer require laravel/jetstream
$ php artisan jetstream:install livewire
$ npm install
$ npm run dev
$ php artisan key:generate
# Criar o banco de dados: testetecnico
$ Alterar: DB_DATABASE=testetecnico
$ Alterar: DB_USERNAME=root
$ Alterar: DB_PASSWORD=pAia1RkewjmwowTO
$ php artisan migrate
$ php artisan db:seed
$ php artisan serve

Login:
    E-mail: admin@admin.com
    Senha: admin


- Baixa o projeto
-


Passo a passo:
<p> $ git clone git@github.com:ruanthiago/testetecnico.git </p>
<p> $ cd testetecnico </p>
<p> $ composer install </p>
<p> $ npm install </p>
<p> $ npm run dev </p>
<p> # Cria o banco de dados mysql: testetecnico </p>
<p> # Alterar os dados no arquivo .env p/ ficar de acordo com a maquina: DB_DATABASE=testetecnico </p>
<p> # Alterar os dados no arquivo .env p/ ficar de acordo com a maquina: DB_USERNAME=root </p>
<p> # Alterar os dados no arquivo .env p/ ficar de acordo com a maquina: DB_PASSWORD=root </p>
<p> $ php artisan key:generate </p>
<p> $ php artisan migrate </p>
<p> $ php artisan db:seed </p>
<p> $ php artisan serve </p>
